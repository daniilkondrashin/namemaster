apiVersion: v1
kind: Service
metadata:
  name: kubernetes-monitor
  namespace: monitoring
  labels:
    app.kubernetes.io/name: kubernetes-monitor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kubernetes-monitor
  ports:
    - name: http
      port: 80
      targetPort: http
